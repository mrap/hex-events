# recipes/test-example.yaml
name: test-example
trigger:
  event: test.event
conditions:
  - field: status
    op: eq
    value: completed
actions:
  - type: shell
    command: echo "hello {{ event.status }}"
