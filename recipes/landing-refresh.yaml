# recipes/landing-refresh.yaml
name: landing-refresh
trigger:
  event: file.modified
conditions:
  - field: path
    op: contains
    value: landings/
actions:
  - type: shell
    command: bash /Users/mrap/hex/.claude/scripts/landings-dashboard.sh --refresh 2>/dev/null || true
